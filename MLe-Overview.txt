Machine Learning (ML) Tutorial
- Übersicht über zu erstellende Jupyter Notebooks

JN01a - Einführung
- Klärung was ML was und was damit gemacht werden kann
  (recognize,predict, Group together, simplify)
- Definition wichtiger Begriffe
  (instances, features, labels, machine learning model)
- Verfahren im ML
  (supervized learning, unsupervized learning,reinforcement learning)
- Supervized laerning
  (training set, test set, error (rate of failure), score (rate of success
   Training error / score, test error / score, generalization error / score,
    rpresentative laerning set)
- Classification vs. Regression
- Unsupervized learning
  (Clustering, dimensionality reduction) 

JN01b - Maschinelles Lernen versus Ableitung
- Beispiel Lineare Regression mit Daten y = m * x + b + Nor(0, u)
- Auch mit Decision Tree durchführen?
- Abbildung 1.10 im Buch Acq23

JN02 - Erste überwachte Modelle
(a) ML Modell 
- Daten aufbereiten
- Daten aufteilen in Trainings- und Testdaten
- ML Methode und seine Parameter auswählen
- Fit durchführen: Die ML Methode auf die Trainingsdaten anwenden
- Vorhersagen: Modell auf die Testdaten anwenden
- Berechnung der Performace durch Anwendung einer geeigneten Bewertungsmethode
- Ergebnis betrachten, Schwachstellen erkennen, Verbesserungen durchführen
(b) Decision Tree
- Daten (habitable Planten) aufbereiten
- Split durchführen
  (Wie berechnet man den besten Split)
- Wie tief soll gesplittet werden?
(c) kNN (k-Nearest Neigbour)
- Daten darstellen (nicht normalisiert / normalisiert)
- Mittelwert vs. Median

JN03a - Überwachte Klassifizierung - Bewertung und Diagnose
- Bewertung bei binärer Klassifikation
  (true positive (TP), true negative (TN), false positive (FP), false negative (FN),
  accuracy, precosion, recall, confusion Matrix
  Receiver operating curve (ROC), Area under the curve (AUC))
- Bewertungsmethode auswählen

JN03b - Cross Validation
- cross Validation (CV)

JN03c - Diagnose
- Overfitting and underfitting
- high bias, high variance
- learning curve
- hyperparameter Tuning
- feature Engineering

JN03d - Multi Klassifikation
